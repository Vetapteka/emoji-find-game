import React, { JSX } from 'react';
import Icon, { IconSizeEnum } from '../icons/Icon';

function UpdateEmojiControl(): JSX.Element {
	return (
		<Icon icon={'UPDATE_ICON'} size={IconSizeEnum.XS}/>
	);
}

export default UpdateEmojiControl;