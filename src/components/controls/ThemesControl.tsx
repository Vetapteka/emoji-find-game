import React, { JSX } from 'react';
import Icon, { IconSizeEnum } from '../icons/Icon';

function ThemesControl(): JSX.Element {
	return (
		<Icon icon="THEME_ICON" size={IconSizeEnum.L}/>
	);
}

export default ThemesControl